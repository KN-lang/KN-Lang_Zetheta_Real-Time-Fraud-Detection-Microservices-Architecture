# Day11 - API - Gateway - Design

The gateway terminates external TLS, validates OAuth tokens, applies route-level authorization, enforces idempotency headers for transaction submission, adds correlation IDs, and forwards approved traffic into the mesh.
