# P2 - Kafka Consumer Lag

Identify lagging consumer group, check partition skew, scale consumers, verify downstream database latency, and pause non-critical replays until lag returns below threshold.
