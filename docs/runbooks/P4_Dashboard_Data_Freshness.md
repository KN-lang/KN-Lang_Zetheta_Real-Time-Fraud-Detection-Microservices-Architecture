# P4 - Dashboard Data Freshness

Check dashboard projector consumer lag, TimescaleDB write latency, Grafana datasource health, and recent schema changes. Rebuild the affected read model from Kafka offsets if projections are stale after service restart.
